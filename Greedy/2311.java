class Solution {
    public int longestSubsequence(String s, int k) {
        // greedy 
        int sum = 0, count = 0;


        for(int i = s.length() - 1; i >= 0; i--) {

        }
    
        return count;
    }
}